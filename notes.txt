С богом :)

по видам чисел: 
    конверсию не делаем.
    если тип не опознан или типы чисел не совпали - никто никуда не едет.

по операциям:
    функции классов (как аргумент принимает другое того же типа число)

по интерфейсу:
    число а с клавиатуры;
    число не-а с клавиатуры;
    операция....? посмотрим насколько сложно будет реализовать кнопки
    кнопка равно - опционально; возможно утоплю её в *операции*

по логированию:
    logging вспомнить;
    отдельная папка;
    разбито по дням.